{
  "name": "bulksend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx src/cli.ts generate",
    "send": "tsx src/cli.ts send",
    "typecheck": "tsc --noEmit",
    "test": "for f in test/*.test.ts; do echo \"--- $f ---\"; npx tsx \"$f\" || exit 1; done"
  },
  "dependencies": {
    "csv-parse": "^7.0.1",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/nodemailer": "^8.0.1",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
